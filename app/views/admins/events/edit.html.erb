<h1><%= @event.name %> の編集</h1>

<%= render 'form', event: @event, url: admins_event_path, submit: '変更を登録する' %>
<div class="col-xs-12">
<%= button_to '削除する', admins_event_path(@event.id), method: :delete, "data-confirm" => "本当に削除しますか？",class: "btn btn-danger" %>
</div>