<%= render 'members/members/profile', member: @member %>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>

     <tr>
        <td><%= attachment_image_tag(@post, :foof_image, :size => "300x300", fallback: "no_image.jpg") %></td>
        <td>　</td>
        <td><%= post.food %></td>
        <td><%= post.content %></td>
      </tr>

    <% end %>
  </tbody>
</table>
